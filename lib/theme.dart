import 'package:flutter/material.dart';

const primaryColor = Color.fromARGB(255, 218, 218, 218);
const backgroundColor = Color.fromARGB(255, 62, 62, 62);

final appTheme =
    ThemeData(primaryColor: primaryColor, backgroundColor: backgroundColor);
